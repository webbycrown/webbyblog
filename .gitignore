# -----------------------------
# WebbyBlog plugin - dist-only repo
# - DO NOT commit source folders (admin/, server/)
# - DO commit built output (dist/)
# -----------------------------

# Dependencies
node_modules/

# Source folders (ignored on purpose)
admin/
server/
# scripts/ is intentionally tracked because it contains build/postinstall/seed helpers

# Build output: keep dist tracked even if parent .gitignore ignores it
!dist/
!dist/**

# Tool caches / temp
build/
.cache/
.tmp/
.turbo/
.eslintcache
.strapi/

# Environment (keep templates if you add them)
.env
.env.*
!.env.example
!.env.template

# Tests / coverage
coverage/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# npm pack artifacts
*.tgz

# Editors / OS
.DS_Store
Thumbs.db
Desktop.ini
.idea/
.vscode/
*.swp
*~
